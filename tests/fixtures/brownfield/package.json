{
  "name": "brownfield-billing",
  "version": "0.1.0",
  "private": true,
  "description": "Fixture brownfield — mini-app de billing em TypeScript (centavos inteiros).",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --test"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
